import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

export default Home = ({ navigation }) => {
  return(
    <View>
      <Text>Placeholder for Feeds </Text>
    </View>
  )
}